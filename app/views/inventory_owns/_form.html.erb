<%= form_for(@inventory_own) do |f| %>
  <% if @inventory_own.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@inventory_own.errors.count, "error") %> prohibited this inventory_own from being saved:</h2>

      <ul>
      <% @inventory_own.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :book_id %><br>
    <%= f.number_field :book_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :condition %><br>
    <%= f.text_field :condition %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
